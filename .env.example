# Local dev — PartyKit dev server runs on port 1999 by default
VITE_PARTYKIT_HOST=localhost:1999

# Production — replace with your deployed PartyKit project host
# VITE_PARTYKIT_HOST=tapbomb.your-username.partykit.dev
